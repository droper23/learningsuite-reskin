{
  "name": "learningsuite-reskin",
  "version": "0.2.0",
  "private": false,
  "description": "A cross-browser userscript that gives BYU LearningSuite an Apple-inspired interface while preserving its original functionality.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "pretest": "node test-build.mjs",
    "test": "node --test dist-test/*.test.js"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "esbuild": "^0.24.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3"
  }
}
